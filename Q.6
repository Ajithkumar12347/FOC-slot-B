#include <stdio.h>

int main() {
    int sum = 0; 
    for (int i = 2; i <= 10; i += 2) { 
        sum += i; 
    }
    printf("The sum of the even numbers from 2 to 10 is: %d\n", sum);
    return 0;
}
