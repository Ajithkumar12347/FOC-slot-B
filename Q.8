#include <stdio.h>

int main() {
    int n = 10;
    int sum = 0;
    int i = 1;
    while(i<=n) {
        if (i % 2 == 0) {
            sum -= i;
        } else {
            sum += i;
        }
        i++;
    }
    printf("The sum of the series 1 to %d is: %d\n", n, sum);
    return 0;
}
